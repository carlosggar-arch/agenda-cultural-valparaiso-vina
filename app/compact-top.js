// Legacy compatibility module.
// The compact layout now loads synchronously from compact-top.css in <head>,
// so older cached pwa.js imports can keep resolving without causing a late
// style injection or a flash of the previous layout.
