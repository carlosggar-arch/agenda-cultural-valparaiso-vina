(() => {
  // Single source of truth for the public PWA release and service-worker cache.
  // v256 generated canonically from source 257e4845431d.
  const RELEASE = 256;
  globalThis.__VIVAMOS_RELEASE__ = RELEASE;
})();
