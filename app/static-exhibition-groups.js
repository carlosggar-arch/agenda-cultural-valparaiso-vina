// Retired compatibility marker.
//
// Grouped exhibition rendering is owned exclusively by exhibition-groups.js.
// This file remains only because the historical CI workflow checks its syntax;
// it must never render, group, observe, fetch or mutate agenda cards.
export const STATIC_EXHIBITION_GROUPS_RETIRED = true;
